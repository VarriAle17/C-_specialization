/* calculate area in meters*/
#include <stdio.h>
#include <math.h>

#define PI 3.16

int main(void)
{
    int r=0, area = 0;
    char ale[] = "pippo";
   /* printf("chitebbi miett'm o ragg \n");
    scanf("%lf", &r);
    area = PI* (r*r);*/
    //int ale;
    //ale = (r+1, r+1);

    printf("l'area e': %d", ale);
    printf("l'area e': %d", r);
    printf("l'area e': %s", &ale);
    return 0;
}
