/* calculate area in meters*/
#include <stdio.h>
#include <math.h>

#define PI 3.16

int main(void)
{
    int a = 1, b = 2, c = 3;
    double r=0, area = 0;
    printf("chitebbi miett'm o ragg \n");
    scanf("%lf", &r);
    area = PI* (r*r);
    printf("l'area e': %lf", area);
    printf("HELLO\t\tWORLD %d\n",  c / a * b);
    return 0;
}
